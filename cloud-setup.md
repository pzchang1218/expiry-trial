# 效期小幫手 — 雲端同步設定教學

## 這是做什麼的？

設定完成後，您的盤點資料會存到**您自己的**免費雲端空間，好處是：

- 換手機、清除瀏覽器資料，**資料都不會消失**
- 手機、平板、電腦**看到同一份資料**，隨時同步
- 店裡多個人一起盤點，互相看得到彼此的進度

**完全免費**（以藥局的資料量，永遠用不完免費額度），資料在您自己的 Google 帳號底下，別人看不到。

**需要準備**：一個 Google 帳號（Gmail 帳號即可）、一台電腦、約 15 分鐘。

---

## 第一部分：建立雲端空間（電腦操作，只需做一次）

### 步驟 1　登入

1. 電腦開啟瀏覽器，網址列輸入：**console.firebase.google.com**
2. 用您的 Google 帳號登入
3. 如果出現歡迎頁面，點「**建立專案**」或「Create a project」

### 步驟 2　建立專案

1. 專案名稱輸入：**pharmacy**（或任何您喜歡的英文名字）
2. 按「**繼續**」
3. 出現「Google Analytics」的頁面 → 把開關**關掉**（不需要）→ 按「**建立專案**」
4. 等待約 30 秒，看到「您的新專案已就緒」→ 按「**繼續**」

### 步驟 3　開啟資料庫

1. 左側選單找到「**建構 Build**」→ 點「**Firestore Database**」
2. 按「**建立資料庫**」
3. 「位置」選擇 **asia-east1（台灣）**
4. 模式選「**以正式版模式啟動**」→ 按「**啟用**」
5. 等待約 30 秒建立完成

### 步驟 4　設定存取規則（重要！）

1. 在 Firestore Database 頁面，點上方的「**規則 Rules**」分頁
2. 您會看到一段程式碼，把**整段全部刪掉**，改貼上以下內容：

```
rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}
```

3. 按右上角「**發布 Publish**」

### 步驟 5　開啟登入功能

1. 左側選單「**建構 Build**」→ 點「**Authentication**」
2. 按「**開始使用**」
3. 在提供者清單中找到「**匿名 Anonymous**」→ 點進去
4. 把開關打開（啟用）→ 按「**儲存**」

### 步驟 6　取得您的兩組代碼

1. 點左上角的**齒輪圖示 ⚙️** → 選「**專案設定**」
2. 停留在「一般 General」分頁，往下滑到「您的應用程式」區塊
3. 點那個 **`</>`** 圖示（網頁應用程式）
4. 應用程式暱稱輸入：**expiry** → 按「**註冊應用程式**」
5. 畫面會出現一段程式碼，裡面有兩行是您需要的：

```
apiKey: "AIzaSyXXXXXXXXXXXXXXXXXXXXXXXXX"      ← 這是「API Key」
projectId: "pharmacy-12345"                     ← 這是「Project ID」
```

6. **把這兩個值抄下來或截圖**（引號裡面的內容才是值）

到這裡，雲端空間就建好了 🎉

---

## 第二部分：在 App 裡啟用（手機或電腦都可以）

1. 打開效期小幫手 → 輸入 PIN 碼
2. 點底部「**📋 盤點記錄**」→ 往下滑到最底
3. 找到「**☁️ 雲端同步設定（進階）**」→ 點開
4. 填入三個欄位：

| 欄位 | 填什麼 |
|---|---|
| **雲端金鑰 API Key** | 步驟 6 抄下的 apiKey |
| **專案 ID Project ID** | 步驟 6 抄下的 projectId |
| **店家代碼** | 自己取一個英文名，例如 `mystore`（**同一間店的所有裝置要填一模一樣**） |

5. 按「**✓ 啟用雲端同步**」
6. 出現綠色的「✅ 雲端同步已啟用」就成功了！

---

## 第三部分：讓第二台裝置也同步

在第二台手機／平板／電腦上：

1. 打開同一個網址、輸入 PIN
2. 一樣進入「☁️ 雲端同步設定」
3. 填入**完全相同的三組值**（API Key、Project ID、店家代碼）
4. 按「✓ 啟用雲端同步」
5. 稍等幾秒，第一台的資料就會自動下載過來 ✨

之後兩台會自動保持同步：登記完約 4 秒自動上傳，每次開啟 App 自動下載。

---

## 常見問題

**Q：出現「驗證失敗」怎麼辦？**
請回頭檢查步驟 5，確認 Authentication 的「匿名」已經**啟用**；以及步驟 6 的 API Key 有沒有複製完整（不要含引號）。

**Q：出現「權限不足 PERMISSION_DENIED」？**
請回頭檢查步驟 4，規則要完全照抄，並且記得按「發布」。

**Q：真的不用錢嗎？**
Firebase 免費方案每天有相當充裕的額度，一般藥局的使用量遠遠用不完。如果哪天真的超過，Google 會直接停止服務而不是自動扣款（除非您主動綁定信用卡升級方案）。

**Q：資料會被誰看到？**
資料存在您自己的 Google 專案裡。API Key 和店家代碼只存在您自己的裝置上，不會公開。建議店家代碼不要用太好猜的名稱。

**Q：沒有網路還能用嗎？**
可以。沒網路時照常掃碼登記，資料先存在手機裡，連上網路後會自動上傳。

**Q：啟用雲端後還需要備份嗎？**
建議每 1～2 個月仍按一次「💾 備份資料」存到雲端硬碟，多一層保險。

---

## 溫馨提醒

- 三組設定值請自行保存好（例如記在手機備忘錄），日後新增裝置會用到
- 「店家代碼」如果填錯，會變成看到另一份空白資料 —— 不會遺失原本的，改正確即可
- 設定過程如果卡住，把畫面截圖下來詢問，通常都是某個開關沒打開

本工具為效期管理輔助用途，不擔保因未及時處理造成之損失。
